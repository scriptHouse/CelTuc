# Entornos y build
.venv*/
__pycache__/
*.pyc
build/
dist/
*.spec
*.egg-info/
.pytest_cache/

# Datos y secretos locales (NUNCA commitear)
# `config*.toml` cubre tambien los configs con nombre propio por sucursal
# (configSalta.toml, configCasaIscha.toml...): TODOS llevan el token del agente.
config*.toml
!config.example.toml
secrets.dat
.env
*.db
*.db-wal
*.db-shm
logs/
